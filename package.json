{
  "name": "secure-app",
  "version": "1.2.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "npm ci --frozen-lockfile && tsc && vite build",
    "audit": "npm audit --audit-level=high"
  },
  "dependencies": {
    "p-queue": "9.0.0",
    "p-retry": "6.2.0",
    "react": "19.0.0",
    "react-dom": "19.0.0",
    "zod": "3.24.2"
  }
}